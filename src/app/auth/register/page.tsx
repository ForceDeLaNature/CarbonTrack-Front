import React from 'react'
import RegisterForm from '@/components/Forms/RegisterForm'

type Props = object

function page({}: Props) {
  return (
    <div>
        <RegisterForm />
    </div>
  )
}

export default page