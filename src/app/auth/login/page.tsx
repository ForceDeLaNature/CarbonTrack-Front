import React from 'react'
import LoginForm from '@/components/Forms/LoginForm'

type Props = object

function page({}: Props) {
  return (
    <div>
      <LoginForm  />
    </div>
  )
}

export default page